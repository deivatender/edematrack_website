import { TermsAndCondition } from './terms-and-condition';

describe('TermsAndCondition', () => {
  it('should create an instance', () => {
    expect(new TermsAndCondition()).toBeTruthy();
  });
});
